<p>日本各地のアイリッシュ・セッションの定番を共有して、セッションをもっと楽しむためのサイト</p>
<%= link_to "about", :about %>
<%= render "shared/search_form" %>
<% if current_user%>
<%= link_to "楽曲登録", new_tune_path %>
<% end %>
<h2>Recent Update</h2>
<dl>
  <% @recents.each do |recent| %>
    <dt>
      <%= link_to recent.tune.name, tune_path(recent.tune) %>
    </dt>
    <dd>
      <%= recent.tune.rhythm %>
      by <%= link_to recent.user.name, tunes_path(user_name: recent.user.name) %>
      <%= link_to "info", info_user_path(recent.user.id) %>
    </dd>
  <% end %>
</dl>