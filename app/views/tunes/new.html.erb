<h2>楽曲登録</h2>
<p class="red strong">「<%= t("memo") %>」と「<%= t("alt_name") %>」以外は必須項目です。</p>

<div class="form">
  <%= form_with model: @tune do |f| %>
    <%= render "shared/tune_form", f: f %>
    <%= f.submit "登録" %>
  <% end %>
</div>