<h2>会員登録</h2>
<p class="red strong">「<%= t("memo") %>」以外は必須項目です。</p>
<div class="form">
  <%= form_with model: @user do |f| %>
    <%= render "shared/user_form", f: f %>
    <%= f.submit "登録" %>
  <% end %>
</div>